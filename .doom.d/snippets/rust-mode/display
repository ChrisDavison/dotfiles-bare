# -*- mode: snippet -*-
# name: Impl Display
# key: display
# condition: t
# --
impl std::fmt::Display for ${1:Type} {
     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
         write!(f, "${2}", self.${3})
     }
}
